name: vlm-in-robotics
channels:
  - defaults
  - conda-forge
  - pytorch
  - nvidia
dependencies:
  - python=3.10
  - numpy=1.24.4
  - compilers
  - cmake
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=12.4
  - cuda-toolkit=12.4
  - ffmpeg
  - pip
  - pip:
    - -e ./simpler_env/ManiSkill2_real2sim
    - -e ./simpler_env
    - -e ./openvla
    - packaging
    - ninja
